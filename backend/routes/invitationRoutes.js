// const express = require("express");
// const router = express.Router();
// const Invitation = require("../models/Invite");
// const authMiddleware = require("../middleware/authMiddleware");
// const { getInvitations, respondToInvite } = require("../controllers/inviteController");

// // Get all invitations for logged-in user
// router.get("/", authMiddleware, getInvitations);

// // Accept / reject an invite
// router.post("/:inviteId/respond", authMiddleware, respondToInvite);

// module.exports = router;
